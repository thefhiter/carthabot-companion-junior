<UserControl x:Class="LearningProject.Views.LearningMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"    
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:syncfusion="clr-namespace:Syncfusion.Windows.PdfViewer;assembly=Syncfusion.PdfViewer.WPF"
             xmlns:iconpack="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
            prism:ViewModelLocator.AutoWireViewModel="True">

    <Border BorderBrush="#FFFFFFFF" Background="#192428" Margin="10 5 10 5"  Grid.Column="1"   BorderThickness="1"  CornerRadius="5">

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <Button Command="{Binding CloseViewCommand}" Cursor="Hand" Margin="20 5"  
                                    VerticalAlignment="Bottom" HorizontalAlignment="Right"
                                    Width="35" Height="35"
                                    Background="Transparent" BorderThickness="2" BorderBrush="White">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <iconpack:PackIconCoolicons Kind="House01" Foreground="White"
                                                     Height="15" Width="15"/>
            </Button>


            <Border x:Name="LoadingBorder" Visibility="Visible" Margin="5" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
                <!-- You can add a spinner or "Loading..." text here -->
                <TextBlock Text="Loading..." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16"/>
            </Border>

            <wv2:WebView2  Margin="5" Grid.Row="1" Source="{Binding source}" NavigationStarting="WebView2_NavigationStarting"  NavigationCompleted="WebView2_NavigationCompleted" />




            <!--<Grid Background="#192428" Grid.Row="1" Margin="10 5 0 5">


            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Border BorderBrush="#FFFFFFFF" Background="#2d383c"   BorderThickness="1" VerticalAlignment="Stretch" CornerRadius="5">
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="Exemples" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="DemiBold" FontSize="30"/>
                    <ScrollViewer Margin="1">
                        <ItemsControl Grid.Row="1"  ItemsSource="{Binding ExempelsList}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Vertical" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border  Cursor="Hand"
                                         Width="200"
                                        Height="50" Margin="5" Background="#414c50"
                                        CornerRadius="2" BorderBrush="White"
                                        BorderThickness="1" VerticalAlignment="Stretch">


                                        <i:Interaction.Triggers>

                                            <i:EventTrigger EventName="MouseLeftButtonDown">
                                                <i:InvokeCommandAction Command="{Binding SelectCommand}"/>
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                        <Grid Margin="0 10 0 5">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="1*"/>
                                                <RowDefinition Height="auto"/>
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="DemiBold" FontSize="15" />
                                            <Border Grid.Row="1" CornerRadius="2" Margin="20 0" >
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Height" Value="0"/>

                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsSelected}" Value="true">
                                                                <Setter Property="Background" Value="#2bc0e8"/>
                                                                <Setter Property="Height" Value="8"/>

                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding IsSelected}" Value="false">
                                                                <Setter Property="Background" Value="Gray"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>

                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                    </ScrollViewer>

                </StackPanel>
            </Border>
            <Border BorderBrush="#FFFFFFFF"  Background="#2d383c" Grid.Column="1" Margin="5 0 5 0"   BorderThickness="1"  CornerRadius="5">
                <Grid >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="1*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="{Binding Title}" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="DemiBold" FontSize="30" Margin="5"/>
                    <syncfusion:PdfViewerControl Grid.Row="1" x:Name="pdfViewer" VerticalAlignment="Stretch" >

                    </syncfusion:PdfViewerControl>
                    <Button Command="{Binding CloseViewCommand}" Cursor="Hand" Margin="20 10"  
                                    VerticalAlignment="Bottom" HorizontalAlignment="Right"
                                    Width="50" Height="50"
                                    Background="Transparent" BorderThickness="3" BorderBrush="White">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="25">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <iconpack:PackIconCoolicons Kind="House01" Foreground="White"
                                                     Height="30" Width="30"/>
                    </Button>
                </Grid>

            </Border>



        </Grid>-->
        </Grid>

    </Border>

</UserControl>
