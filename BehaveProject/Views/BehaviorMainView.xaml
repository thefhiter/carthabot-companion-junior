<UserControl x:Class="BehaveProject.Views.BehaviorMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"   
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf" 
             Loaded="UserControl_Loaded"
             xmlns:iconpack="http://metro.mahapps.com/winfx/xaml/iconpacks"
             prism:ViewModelLocator.AutoWireViewModel="True">



    <Grid Background="#192428" Margin="5 10 5 5" >
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <Border Margin="50" Grid.Row="1" BorderBrush="Gray" BorderThickness="3" CornerRadius="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="0.40*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Margin="5" BorderBrush="Gray" BorderThickness="2" VerticalAlignment="Stretch" CornerRadius="10">
                    <ItemsControl Grid.Column="1"  Margin="5 5" VerticalAlignment="Center" HorizontalAlignment="Stretch"  ItemsSource="{Binding Modes}">

                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Vertical" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Background="Transparent" >
                                    <Grid.Style>
                                        <Style TargetType="Grid">
                                            <Setter Property="Visibility" Value="Hidden"/>

                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected}" Value="true">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsSelected}" Value="false">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Grid.Style>
                                    <Image Source="{Binding ImagePath}"  Margin="5"
                                           />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                </Border>

                <Border Grid.Column="1" Margin="5" BorderBrush="Gray" BorderThickness="2" CornerRadius="10">

                    <ItemsControl Grid.Column="1"  Margin="5 5" HorizontalAlignment="Stretch"  ItemsSource="{Binding Modes}">

                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Vertical" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Background="Transparent" Cursor="Hand">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="MouseLeftButtonDown">
                                            <i:InvokeCommandAction Command="{Binding SelectedCommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="auto"/>
                                        <ColumnDefinition Width="1*"/>
                                    </Grid.ColumnDefinitions>


                                    <Grid Grid.Column="1">
                                        <Grid Grid.Column="1" >
                                            <Grid.Style>
                                                <Style TargetType="Grid">
                                                    <Setter Property="Visibility" Value="Hidden"/>
                                                    
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSelected}" Value="true">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsSelected}" Value="false">
                                                            <Setter Property="Height" Value="0"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Grid.Style>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="1*"/>
                                                <RowDefinition Height="1*"/>
                                            </Grid.RowDefinitions>

                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="auto"/>
                                                <ColumnDefinition Width="1*"/>
                                            </Grid.ColumnDefinitions>
                                            <Border Background="{Binding Color}"  Grid.RowSpan="2" Margin="0 0 5 0"
                                           
                                                BorderThickness="4" VerticalAlignment="Stretch"  CornerRadius="5" Width="20">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="BorderBrush" Value="#192428"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsSelected}" Value="true">

                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>

                                            </Border>
                                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Name}" Foreground="{Binding Color}" 
                                                   VerticalAlignment="Center" 
                                                   HorizontalAlignment="Left" FontWeight="Bold" FontSize="30"/>
                                            <TextBlock TextWrapping="Wrap" Grid.Column="1" Grid.Row="1" Text="{Binding Description}" 
                                                   VerticalAlignment="Center" 
                                                   HorizontalAlignment="Left" FontWeight="Normal" FontSize="18"/>
                                        </Grid>
                                        <Grid Grid.Column="1">
                                            <Grid.Style>
                                                <Style TargetType="Grid">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>

                                                        <DataTrigger Binding="{Binding IsSelected}" Value="true">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Grid.Style>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="1*"/>
                                            </Grid.RowDefinitions>

                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="auto"/>
                                                <ColumnDefinition Width="1*"/>
                                            </Grid.ColumnDefinitions>
                                            <Border Background="{Binding Color}"   Margin="0 0 5 0" MinHeight="70"
                                           
                                                BorderThickness="4" VerticalAlignment="Stretch"  CornerRadius="5" Width="20">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="BorderBrush" Value="#192428"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsSelected}" Value="true">

                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>

                                            </Border>
                                            
                                            <TextBlock Grid.Column="1" Text="{Binding Name}" Foreground="{Binding Color}" 
                                                   VerticalAlignment="Center" 
                                                   HorizontalAlignment="Left" FontWeight="Bold" FontSize="30"/>
                                        </Grid>
                                    </Grid>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>


                
                </Border>

            </Grid>


        </Border>
        <Grid Grid.Row="1" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="5*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>


            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <Border Cursor="Hand" Background="Gray" Opacity="0.5" Width="30" Margin="3 0" CornerRadius="10 0 0 10 ">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="MouseLeftButtonDown">
                            <i:InvokeCommandAction Command="{Binding GoLeftCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                    <iconpack:PackIconEvaIcons Kind="ArrowLeft" Foreground="#192428" Width="30" Height="30" HorizontalAlignment="center" VerticalAlignment="center"/>
                </Border>


                <Border Cursor="Hand" Grid.Column="2" Background="Gray" Opacity="0.5" Width="30" Margin="3 0" CornerRadius="0 10 10 0 ">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="MouseLeftButtonDown">
                            <i:InvokeCommandAction Command="{Binding GoRightCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                    <iconpack:PackIconEvaIcons Kind="ArrowRight" Foreground="#192428" Width="30" Height="30" HorizontalAlignment="center" VerticalAlignment="center"/>
                </Border>
                
                <Border BorderBrush="{Binding SelecetdMode.Color}" Grid.Column="1" BorderThickness="3" CornerRadius="0" Margin="0">

                </Border>
            </Grid>


            <Border BorderBrush="Gray" BorderThickness="2" Grid.Row="1" Margin="5" CornerRadius="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>



                    <ItemsControl Grid.Column="1"  Margin="10 5" HorizontalAlignment="center"  ItemsSource="{Binding Modes}">

                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{Binding Color}"  BorderThickness="4" Margin="5 0" CornerRadius="10" Width="200">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderBrush" Value="#192428"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected}" Value="true">
                                                    <Setter Property="BorderBrush" Value="White"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Name}" Foreground="#192428" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold" FontSize="20"/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>



                </Grid>
            </Border>
        </Grid >

        <!--<syncfusion:Carousel  Grid.Row="1" 
                Cursor="Hand"
                EnableRotationAnimation="True"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                x:Name="carousel"
                ItemsPerPage="3"
                EnableLooping="true"
                ItemsSource="{Binding Modes}"
                OpacityFraction="0"
                VisualMode="CustomPath"
                              
                              
                RotationSpeed="1">
            <syncfusion:Carousel.Path>
                <Path Data="M0,300 L600,300"/>
            </syncfusion:Carousel.Path>
            <syncfusion:Carousel.ItemTemplate>
                <DataTemplate>
                    <Border Cursor="Hand" Margin="-600 0"  VerticalAlignment="Stretch" HorizontalAlignment="Stretch">

                        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                            <TextBlock Grid.Row="0" Text="{Binding Name}" TextWrapping="Wrap" TextAlignment="Left"
                                                Foreground="White"  
                                                FontSize="30" VerticalAlignment="Center"
                                                HorizontalAlignment="Left" FontWeight="DemiBold"/>

                            <Border  MinWidth="1200" MinHeight="700" CornerRadius="15"  >
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#414c50"/>
                                        <Setter Property="BorderBrush" Value="{Binding Color}"/>
                                        <Setter Property="BorderThickness" Value="4"/>

                                    </Style>
                                </Border.Style>

                            </Border>
                        </StackPanel>


                    </Border>



                </DataTemplate>
            </syncfusion:Carousel.ItemTemplate>
        </syncfusion:Carousel>-->

        <Button Command="{Binding CloseViewCommand}" Cursor="Hand" Margin="20 5"  
                                    VerticalAlignment="Bottom" HorizontalAlignment="Right"
                                    Width="50" Height="50"
                                    Background="Transparent" BorderThickness="3" BorderBrush="White">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="25">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
            <iconpack:PackIconCoolicons Kind="House01" Foreground="White"
                                                     Height="30" Width="30"/>
        </Button>

    </Grid>
</UserControl>
 