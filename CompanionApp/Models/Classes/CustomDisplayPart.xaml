<core:NotificationDisplayPart x:Class="CompanionApp.Models.Classes.CustomDisplayPart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconpack="http://metro.mahapps.com/winfx/xaml/iconpacks"                 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CompanionApp.Models.Classes"
             xmlns:core="clr-namespace:ToastNotifications.Core;assembly=ToastNotifications"
             mc:Ignorable="d"  Background="Transparent" Height="100"
             d:DataContext="{d:DesignInstance local:CustomNotification, IsDesignTimeCreatable=False}" >
    <!--<Border >
    </Border>-->
    <Grid >
        <Border Margin="5" Background="#0060ac" BorderThickness="2" BorderBrush="White" CornerRadius="10 10 0 10">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" >
                        <Image Margin="0 0 5 0" Source="/Resources/Images/up-arrow.png" Width="27" Height="27"  VerticalAlignment="Center" HorizontalAlignment="left"/>

                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{Binding Message}" Grid.Column="1"   HorizontalAlignment="Left" FontSize="15" FontWeight="DemiBold" Foreground="White" TextWrapping="WrapWithOverflow" VerticalAlignment="Center" />
                            <TextBlock Text="{Binding Version}" Grid.Column="1"  HorizontalAlignment="Left" FontSize="13" FontWeight="DemiBold" Foreground="LightGray" Grid.Row="1" TextWrapping="WrapWithOverflow" VerticalAlignment="Top" />

                        </StackPanel>
                    </StackPanel>

                </StackPanel>

                <Button Background="Transparent" Command="{Binding UpdateCommand}"  Cursor="Hand" VerticalAlignment="Bottom" Grid.Row="1" BorderThickness="0" HorizontalAlignment="Right">
                    <TextBlock Margin="4 0 0 0"
                                       FontWeight="DemiBold"
                                       FontSize="15"
                                       TextDecorations="Underline"
                         VerticalAlignment="Center"
                         Text="Update" />
                </Button>
            </Grid>
        </Border>
        
        

    </Grid>

</core:NotificationDisplayPart>