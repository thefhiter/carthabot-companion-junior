<mah:MetroWindow x:Class="CompanionApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:controls="clr-namespace:DMSkin.WPF.Controls;assembly=DMSkin.WPF"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Background="Transparent"
        Title="{Binding Title}"
        xmlns:iconpack="http://metro.mahapps.com/winfx/xaml/iconpacks"        
        TitleCharacterCasing="Normal"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        Foreground="White"   
        ResizeMode="CanResize"
        WindowState="Maximized"  
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"

        Loaded="MetroWindow_Loaded"
        WindowStartupLocation="CenterScreen">


    <mah:MetroWindow.RightWindowCommands>
        <mah:WindowCommands>
            <Grid>
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsUpToDate}" Value="false">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <Button Command="{Binding UpdateCommand}" Content="{Binding UpdateContent}" Cursor="Hand" >

                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <iconpack:PackIconFontAwesome Width="15" 
                                        Height="15"
                                        VerticalAlignment="Center"
                                        Kind="ArrowUpLongSolid" />
                                <TextBlock Margin="4 0 0 0"
                                       FontWeight="DemiBold"
                                       FontSize="15"
                                       TextDecorations="Underline"
                         VerticalAlignment="Center"
                         Text="{Binding}" />
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>
            </Grid>
            <ComboBox Margin="5 1"  BorderThickness="0" Background="Transparent"    ItemsSource="{Binding Languages}" SelectedIndex="{Binding SelectedIndex}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" >
                            <Image Source="{Binding Image}" Margin="0 0 5 0" Width="18" Height="18" />
                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontWeight="DemiBold" FontSize="16" />
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </mah:WindowCommands>
    </mah:MetroWindow.RightWindowCommands>
    

    
    <Grid Background="#192428">
        <Border  CornerRadius="5"  BorderThickness="0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="12" ShadowDepth="0" Color="#88000000"/>
            </Border.Effect>
            <Grid Margin="0,0,0,0">
                <Grid Background="Transparent">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                        <RowDefinition Height="30"></RowDefinition>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Name="DMTitle">
                    </Grid>
                </Grid>
            </Grid>
        </Border>
        <Grid >
            <Grid.RowDefinitions>
                <RowDefinition />
            </Grid.RowDefinitions>
            <ContentControl Grid.Row="1" x:Name="MainRegion" prism:RegionManager.RegionName="MainRegion" VerticalAlignment="Stretch"/>

        </Grid>
        <ResizeGrip Style="{StaticResource WyyResizeGrip}" />
    </Grid>
</mah:MetroWindow>
