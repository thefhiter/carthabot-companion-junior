<UserControl x:Class="CompanionApp.Views.AtelierView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:iconpack="http://metro.mahapps.com/winfx/xaml/iconpacks"
             prism:ViewModelLocator.AutoWireViewModel="True">



    <Grid Visibility="{Binding IsVisible}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="1" Margin="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <WrapPanel>

                    


                    <Border Cursor="Hand" Margin="5" VerticalAlignment="Stretch">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="#414c50"/>
                                <Setter Property="Effect" Value="{x:Null}"/>
                                <Setter Property="BorderBrush" Value="DarkGray"/>
                                <Setter Property="BorderThickness" Value="3"/>
                                <Setter Property="CornerRadius" Value="15"/>
                                <Setter Property="Width" Value="250"/>
                                <Setter Property="Height" Value="250"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Atelier[1].OnHovered}" Value="true">
                                        <Setter Property="BorderBrush" Value="{Binding Atelier[1].Color}"/>
                                        <Setter Property="BorderThickness" Value="3"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>


                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="MouseLeftButtonDown">
                                <i:InvokeCommandAction Command="{Binding Atelier[1].SelectedCommand}"/>
                            </i:EventTrigger>
                            <i:EventTrigger EventName="MouseEnter">
                                <i:InvokeCommandAction Command="{Binding Atelier[1].BorderHoverCommand}"/>
                            </i:EventTrigger>
                            <i:EventTrigger EventName="MouseLeave">
                                <i:InvokeCommandAction Command="{Binding Atelier[1].BorderLeaveCommand}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        
                        <!-- Content -->
                        <Grid Margin="10 20 10 10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="1*"/>
                                <RowDefinition Height="1*"/>
                            </Grid.RowDefinitions>

                            <Image Source="../Resources/Images/Icons/mode.png" Width="75" Height="75" HorizontalAlignment="Left" VerticalAlignment="Center"/>

                            <TextBlock Grid.Row="1" Grid.ColumnSpan="2"
                                           FontWeight="DemiBold"
                                           TextWrapping="Wrap" 
                                           TextAlignment="Left"
                                           FontSize="20"
                                           Text="{DynamicResource behav}" 
                                           VerticalAlignment="Center" 
                                           HorizontalAlignment="Left">
                            </TextBlock>

                            <TextBlock Grid.Row="2" Grid.ColumnSpan="2"
                                       VerticalAlignment="Center"
                                           FontWeight="Normal"
                                           TextWrapping="Wrap" 
                                           FontSize="14"
                                           Text="{DynamicResource flashModes}" 
                                           >
                            </TextBlock>
                        </Grid>
                    </Border>

                    <Border Cursor="Hand" Margin="5" VerticalAlignment="Stretch">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="#414c50"/>
                                <Setter Property="Effect" Value="{x:Null}"/>
                                <Setter Property="BorderBrush" Value="DarkGray"/>
                                <Setter Property="BorderThickness" Value="3"/>
                                <Setter Property="CornerRadius" Value="15"/>
                                <Setter Property="Width" Value="250"/>
                                <Setter Property="Height" Value="250"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Atelier[0].OnHovered}" Value="true">
                                        <Setter Property="BorderBrush" Value="{Binding Atelier[0].Color}"/>
                                        <Setter Property="BorderThickness" Value="3"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>


                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="MouseLeftButtonDown">
                                <i:InvokeCommandAction Command="{Binding Atelier[0].SelectedCommand}"/>
                            </i:EventTrigger>
                            <i:EventTrigger EventName="MouseEnter">
                                <i:InvokeCommandAction Command="{Binding Atelier[0].BorderHoverCommand}"/>
                            </i:EventTrigger>
                            <i:EventTrigger EventName="MouseLeave">
                                <i:InvokeCommandAction Command="{Binding Atelier[0].BorderLeaveCommand}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        
                        <!-- Content -->
                        <Grid Margin="10 20 10 10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="1*"/>
                                <RowDefinition Height="1*"/>
                            </Grid.RowDefinitions>

                            <Image Source="../Resources/Images/Icons/puzzle.png" Width="75" Height="75" HorizontalAlignment="Left" VerticalAlignment="Center"/>

                            <TextBlock Grid.Row="1" Grid.ColumnSpan="2"
                                           FontWeight="DemiBold"
                                           TextWrapping="Wrap" 
                                           TextAlignment="Left"
                                           FontSize="20"
                                           Text="{DynamicResource learn}" 
                                           VerticalAlignment="Center" 
                                           HorizontalAlignment="Left">
                            </TextBlock>

                            <TextBlock Grid.Row="2" Grid.ColumnSpan="2" VerticalAlignment="Center"
                                           FontWeight="Normal"
                                           TextWrapping="Wrap" 
                                           FontSize="14"
                                           Text="{DynamicResource learnVisualProgramming}" 
                                           >
                            </TextBlock>
                        </Grid>
                    </Border>

                    <Border Cursor="Hand" Margin="5" VerticalAlignment="Stretch">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="#414c50"/>
                                <Setter Property="Effect" Value="{x:Null}"/>
                                <Setter Property="BorderBrush" Value="DarkGray"/>
                                <Setter Property="BorderThickness" Value="3"/>
                                <Setter Property="CornerRadius" Value="15"/>
                                <Setter Property="Width" Value="250"/>
                                <Setter Property="Height" Value="250"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Atelier[2].OnHovered}" Value="true">
                                        <Setter Property="BorderBrush" Value="{Binding Atelier[2].Color}"/>
                                        <Setter Property="BorderThickness" Value="3"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>


                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="MouseLeftButtonDown">
                                <i:InvokeCommandAction Command="{Binding Atelier[2].SelectedCommand}"/>
                            </i:EventTrigger>
                            <i:EventTrigger EventName="MouseEnter">
                                <i:InvokeCommandAction Command="{Binding Atelier[2].BorderHoverCommand}"/>
                            </i:EventTrigger>
                            <i:EventTrigger EventName="MouseLeave">
                                <i:InvokeCommandAction Command="{Binding Atelier[2].BorderLeaveCommand}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        
                        <!-- Content -->
                        <Grid Margin="10 20 10 10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="1*"/>
                                <RowDefinition Height="1*"/>
                            </Grid.RowDefinitions>

                            <Image Source="../Resources/Images/Icons/python.png" Width="75" Height="75" HorizontalAlignment="Left" VerticalAlignment="Center"/>

                            <TextBlock Grid.Row="1" Grid.ColumnSpan="2"
                                           FontWeight="DemiBold"
                                           TextWrapping="Wrap" 
                                           TextAlignment="Left"
                                           FontSize="20"
                                           Text="{DynamicResource python}" 
                                           VerticalAlignment="Center" 
                                           HorizontalAlignment="Left">
                            </TextBlock>

                            <TextBlock Grid.Row="2" Grid.ColumnSpan="2" VerticalAlignment="Center"
                                           FontWeight="Normal"
                                           TextWrapping="Wrap"
                                           FontSize="14"
                                           Text="{DynamicResource advancedPython}"
                                           >
                            </TextBlock>
                        </Grid>
                    </Border>

                    <!-- Coding (under 6) – Thymio-style VPL studio -->
                    <Border Cursor="Hand" Margin="5" VerticalAlignment="Stretch">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="#414c50"/>
                                <Setter Property="Effect" Value="{x:Null}"/>
                                <Setter Property="BorderBrush" Value="DarkGray"/>
                                <Setter Property="BorderThickness" Value="3"/>
                                <Setter Property="CornerRadius" Value="15"/>
                                <Setter Property="Width" Value="250"/>
                                <Setter Property="Height" Value="250"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Atelier[4].OnHovered}" Value="true">
                                        <Setter Property="BorderBrush" Value="{Binding Atelier[4].Color}"/>
                                        <Setter Property="BorderThickness" Value="3"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>


                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="MouseLeftButtonDown">
                                <i:InvokeCommandAction Command="{Binding Atelier[4].SelectedCommand}"/>
                            </i:EventTrigger>
                            <i:EventTrigger EventName="MouseEnter">
                                <i:InvokeCommandAction Command="{Binding Atelier[4].BorderHoverCommand}"/>
                            </i:EventTrigger>
                            <i:EventTrigger EventName="MouseLeave">
                                <i:InvokeCommandAction Command="{Binding Atelier[4].BorderLeaveCommand}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>

                        <!-- Content -->
                        <Grid Margin="10 20 10 10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="1*"/>
                                <RowDefinition Height="1*"/>
                            </Grid.RowDefinitions>

                            <iconpack:PackIconFontAwesome Kind="PuzzlePieceSolid" Width="70" Height="70" Foreground="#EE7B2F" HorizontalAlignment="Left" VerticalAlignment="Center"/>

                            <TextBlock Grid.Row="1" Grid.ColumnSpan="2"
                                           FontWeight="DemiBold"
                                           TextWrapping="Wrap"
                                           TextAlignment="Left"
                                           FontSize="20"
                                           Text="{DynamicResource vplCard}"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Left">
                            </TextBlock>

                            <TextBlock Grid.Row="2" Grid.ColumnSpan="2" VerticalAlignment="Center"
                                           FontWeight="Normal"
                                           TextWrapping="Wrap"
                                           FontSize="14"
                                           Text="{DynamicResource vplCardDesc}"
                                           >
                            </TextBlock>
                        </Grid>
                    </Border>

                    <!-- Draw with a pen – turtle plotter -->
                    <Border Cursor="Hand" Margin="5" VerticalAlignment="Stretch">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="#414c50"/>
                                <Setter Property="Effect" Value="{x:Null}"/>
                                <Setter Property="BorderBrush" Value="DarkGray"/>
                                <Setter Property="BorderThickness" Value="3"/>
                                <Setter Property="CornerRadius" Value="15"/>
                                <Setter Property="Width" Value="250"/>
                                <Setter Property="Height" Value="250"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Atelier[5].OnHovered}" Value="true">
                                        <Setter Property="BorderBrush" Value="{Binding Atelier[5].Color}"/>
                                        <Setter Property="BorderThickness" Value="3"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>

                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="MouseLeftButtonDown">
                                <i:InvokeCommandAction Command="{Binding Atelier[5].SelectedCommand}"/>
                            </i:EventTrigger>
                            <i:EventTrigger EventName="MouseEnter">
                                <i:InvokeCommandAction Command="{Binding Atelier[5].BorderHoverCommand}"/>
                            </i:EventTrigger>
                            <i:EventTrigger EventName="MouseLeave">
                                <i:InvokeCommandAction Command="{Binding Atelier[5].BorderLeaveCommand}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>

                        <!-- Content -->
                        <Grid Margin="10 20 10 10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="1*"/>
                                <RowDefinition Height="1*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="✏️" FontFamily="Segoe UI Emoji" FontSize="58" HorizontalAlignment="Left" VerticalAlignment="Center"/>

                            <TextBlock Grid.Row="1" Grid.ColumnSpan="2"
                                           FontWeight="DemiBold"
                                           TextWrapping="Wrap"
                                           TextAlignment="Left"
                                           FontSize="20"
                                           Text="{DynamicResource drawCard}"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Left">
                            </TextBlock>

                            <TextBlock Grid.Row="2" Grid.ColumnSpan="2" VerticalAlignment="Center"
                                           FontWeight="Normal"
                                           TextWrapping="Wrap"
                                           FontSize="14"
                                           Text="{DynamicResource drawCardDesc}"
                                           >
                            </TextBlock>
                        </Grid>
                    </Border>

                </WrapPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
